# Install dependencies with `npm install`

# Run `npm test` to run all mocha tests

# This code solveds the problem of creating a function that will return an array with all zeroes at the end of the array if zeroes exist
